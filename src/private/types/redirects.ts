export interface accountsInterface {
    ip: string
    password: string
    user: {
        name: string
        acc: string
    }
}

export interface redirectsInterface {
    name: string
    redirect: string
}